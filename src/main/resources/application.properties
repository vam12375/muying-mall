spring.application.name=muying-mall


# CORS配置
cors.allowed-origins=http://localhost:5173,http://127.0.0.1:5173,http://localhost:3000
cors.allowed-methods=*
cors.allowed-headers=*
cors.allow-credentials=true
cors.max-age=3600

# 文件上传配置
upload.path=E:/11/muying-web/public
upload.avatar.path=/avatars
upload.domain=http://localhost:5173

# 文件访问URL（保持兼容性）
file.upload.path=E:/11/muying-web/public
file.access.url=http://localhost:5173/avatars

# 文件上传大小限制配置
spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=10MB
