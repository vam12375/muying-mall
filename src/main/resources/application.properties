spring.application.name=muying-mall


# CORS配置 - 临时添加生产域名用于快速验证
cors.allowed-origins=http://localhost:5173,http://127.0.0.1:5173,http://localhost:3000,https://muying-web.pages.dev,https://pig.pig1.de5.net
cors.allowed-methods=*
cors.allowed-headers=*
cors.allow-credentials=true
cors.max-age=3600

# 文件上传配置
upload.path=G:/muying/muying-web/public
upload.avatar.path=/avatars
upload.domain=http://localhost:5173

# 文件访问URL（保持兼容性）
file.upload.path=G:/muying/muying-web/public
file.access.url=http://localhost:5173/avatars

# 文件上传大小限制配置
spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=10MB
