<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.muyingmall.mapper.ProductSkuMapper">

    <!-- 结果映射 -->
    <resultMap id="BaseResultMap" type="com.muyingmall.entity.ProductSku">
        <id column="sku_id" property="skuId"/>
        <result column="product_id" property="productId"/>
        <result column="sku_code" property="skuCode"/>
        <result column="sku_name" property="skuName"/>
        <result column="spec_values" property="specValues"/>
        <result column="price" property="price"/>
        <result column="stock" property="stock"/>
        <result column="sku_image" property="skuImage"/>
        <result column="weight" property="weight"/>
        <result column="volume" property="volume"/>
        <result column="status" property="status"/>
        <result column="sort_order" property="sortOrder"/>
        <result column="version" property="version"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <!-- 根据商品ID查询SKU列表 -->
    <select id="selectByProductId" resultMap="BaseResultMap">
        SELECT *
        FROM product_sku
        WHERE product_id = #{productId}
        ORDER BY sort_order ASC, sku_id ASC
    </select>

    <!-- 根据SKU编码查询 -->
    <select id="selectBySkuCode" resultMap="BaseResultMap">
        SELECT *
        FROM product_sku
        WHERE sku_code = #{skuCode}
        LIMIT 1
    </select>

    <!-- 扣减库存（使用乐观锁） -->
    <update id="deductStock">
        UPDATE product_sku
        SET stock = stock - #{quantity},
            version = version + 1
        WHERE sku_id = #{skuId}
          AND stock >= #{quantity}
          AND version = #{version}
    </update>

    <!-- 恢复库存 -->
    <update id="restoreStock">
        UPDATE product_sku
        SET stock = stock + #{quantity},
            version = version + 1
        WHERE sku_id = #{skuId}
    </update>

    <!-- 批量更新库存 -->
    <update id="batchUpdateStock">
        <foreach collection="list" item="item" separator=";">
            UPDATE product_sku
            SET stock = #{item.stock},
                version = version + 1
            WHERE sku_id = #{item.skuId}
        </foreach>
    </update>

    <!-- 查询库存不足的SKU列表 -->
    <select id="selectLowStockSkus" resultMap="BaseResultMap">
        SELECT ps.*, p.product_name
        FROM product_sku ps
        LEFT JOIN product p ON ps.product_id = p.product_id
        WHERE ps.stock <![CDATA[ <= ]]> #{threshold}
          AND ps.status = 1
        ORDER BY ps.stock ASC
    </select>

    <!-- 根据商品ID删除SKU -->
    <delete id="deleteByProductId">
        DELETE FROM product_sku
        WHERE product_id = #{productId}
    </delete>

</mapper>
